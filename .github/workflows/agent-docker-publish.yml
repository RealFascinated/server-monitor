name: Publish Agent Docker image

on:
  push:
    branches:
      - master
    paths:
      - "agent/**"
      - ".github/workflows/agent-docker-publish.yml"
    tags:
      - "agent/v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Optional release tag (e.g. agent/v2.0.0). Leave empty to build :master from the selected branch."
        required: false
        type: string
        default: ""

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_REPOSITORY: ghcr.io/realfascinated/monitor-agent

defaults:
  run:
    working-directory: agent

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.tag != '' && format('refs/tags/{0}', github.event.inputs.tag) || github.ref }}

      - name: Resolve build
        id: build
        shell: bash
        working-directory: ${{ github.workspace }}
        run: |
          set -euo pipefail
          image="${{ env.IMAGE_REPOSITORY }}"
          echo "image=$image" >> "$GITHUB_OUTPUT"

          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            tag="${{ github.event.inputs.tag }}"
            tag="${tag//[[:space:]]/}"
            if [ -z "$tag" ]; then
              version="master-$(git rev-parse --short HEAD)"
              {
                echo "mode=master"
                echo "version=$version"
                echo "tags<<EOF"
                echo "${image}:master"
                echo "EOF"
                echo "nvidia_tags<<EOF"
                echo "${image}:nvidia-master"
                echo "EOF"
              } >> "$GITHUB_OUTPUT"
              exit 0
            fi
            if [[ ! "$tag" =~ ^agent/v[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.-]+)?$ ]]; then
              echo "Invalid agent release tag: $tag"
              echo "Expected format: agent/vMAJOR.MINOR.PATCH"
              exit 1
            fi
            version="${tag#agent/v}"
            {
              echo "mode=release"
              echo "version=$version"
              echo "tags<<EOF"
              echo "${image}:${version}"
              echo "${image}:latest"
              echo "EOF"
              echo "nvidia_tags<<EOF"
              echo "${image}:${version}-nvidia"
              echo "${image}:nvidia"
              echo "EOF"
            } >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if [ "${{ github.ref }}" = "refs/heads/master" ]; then
            version="master-$(git rev-parse --short HEAD)"
            {
              echo "mode=master"
              echo "version=$version"
              echo "tags<<EOF"
              echo "${image}:master"
              echo "EOF"
              echo "nvidia_tags<<EOF"
              echo "${image}:nvidia-master"
              echo "EOF"
            } >> "$GITHUB_OUTPUT"
            exit 0
          fi

          tag="${GITHUB_REF#refs/tags/}"
          if [[ ! "$tag" =~ ^agent/v[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.-]+)?$ ]]; then
            echo "Unexpected ref: ${GITHUB_REF}"
            exit 1
          fi
          version="${tag#agent/v}"
          {
            echo "mode=release"
            echo "version=$version"
            echo "tags<<EOF"
            echo "${image}:${version}"
            echo "${image}:latest"
            echo "EOF"
            echo "nvidia_tags<<EOF"
            echo "${image}:${version}-nvidia"
            echo "${image}:nvidia"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata labels
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ steps.build.outputs.image }}

      - name: Build and push (default)
        uses: docker/build-push-action@v6
        with:
          context: agent
          file: agent/Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          build-args: |
            VERSION=${{ steps.build.outputs.version }}
          tags: ${{ steps.build.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: Build and push (NVIDIA)
        uses: docker/build-push-action@v6
        with:
          context: agent
          file: agent/Dockerfile.nvidia
          push: true
          platforms: linux/amd64,linux/arm64
          build-args: |
            VERSION=${{ steps.build.outputs.version }}
          tags: ${{ steps.build.outputs.nvidia_tags }}
          labels: ${{ steps.meta.outputs.labels }}
